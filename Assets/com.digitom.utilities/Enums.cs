namespace DigitomUtilities
{
    public enum DirectionSpace { Self, World, Camera }
    public enum OrderType { None, Ascending, Descending }
    public enum AffectedType { Sender, Receiver }
    public enum ReferenceType { Constant, Instance, Interface }
    public enum MonoUpdateType { Update, FixedUpdate, LateUpdate }
}
